<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amor.memberSQL">
<select id="memberLogin" resultType="com.amor.member.model.MemberDTO" parameterType="String">
	select member_pwd, member_quit, member_block from member 
	where member_id=#{id} 
</select>

<select id="userIdFind" resultType="com.amor.member.model.MemberDTO" parameterType="java.util.HashMap">
	select * from member where member_name=#{member_name} and member_email=#{member_email}
</select>
</mapper>